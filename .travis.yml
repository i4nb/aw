language: node_js
node_js:
  - "0.10"
install:
  - npm install phantomjs -g
  - npm install -g mocha-phantomjs
  - npm install -g gulp
  - npm install
before_script:
  - phantomjs --version
  - mocha-phantomjs --version
  - gulp test
script:
  - gulp test
